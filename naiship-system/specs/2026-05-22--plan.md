# 奈拾管理系統第二批 7 項 UX 優化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 實作 7 項 UX 優化：行事曆跟進按鈕、里程碑 6 期模板、案件刪除、工種付款進度顏色、Gantt 狀態 chip 篩選、工作日誌週報匯出、廠商評分備注。

**Architecture:** 所有改動皆為現有 Vue 3 SFC 的局部修改，無需新建 store 或路由。最重要的改動為 `cases.js` 新增 `deleteCase()` 供 `CaseEditModal.vue` 呼叫。功能間相互獨立，可逐一 commit。

**Tech Stack:** Vue 3 Composition API、Pinia、Firebase Firestore、Tailwind CSS、xlsx（已安裝）

---

## 檔案修改清單

| 檔案 | 任務 |
|------|------|
| `src/stores/cases.js` | Task 1：新增 deleteCase |
| `src/components/cases/CaseEditModal.vue` | Task 2：刪除按鈕 + 二次確認 |
| `src/components/cases/PaymentMilestones.vue` | Task 3：套用模板 Modal |
| `src/components/cases/GanttTab.vue` | Task 4：狀態篩選 chip |
| `src/components/cases/WorkTypePanel.vue` | Task 5：付款進度顏色 |
| `src/components/clients/ClientDetail.vue` | Task 6：加入行事曆按鈕 |
| `src/components/cases/CalendarTab.vue` | Task 6：新增 followup 事件類型 |
| `src/components/settings/VendorManager.vue` | Task 7：評分與備注 |
| `src/components/cases/WorkJournalTab.vue` | Task 8：週報匯出 Excel |

---

## Task 1：cases.js 新增 deleteCase

**Files:**
- Modify: `naiship-system/src/stores/cases.js`

- [ ] **Step 1：在 cases.js 新增 deleteCase 並 export**

完整修改後的 `src/stores/cases.js`：

```js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { collection, query, where, orderBy, onSnapshot, addDoc, updateDoc, deleteDoc, getDocs, doc, serverTimestamp } from 'firebase/firestore'
import { db } from '@/firebase'

export const useCasesStore = defineStore('cases', () => {
    const cases = ref([])
    let unsubscribe = null

    function subscribe(companyIds) {
        const valid = (companyIds ?? []).filter(Boolean)
        if (valid.length === 0) return
        if (unsubscribe) unsubscribe()
        const q = query(
            collection(db, 'cases'),
            where('companyId', 'in', valid),
            orderBy('createdAt', 'desc')
        )
        unsubscribe = onSnapshot(q, (snap) => {
            cases.value = snap.docs.map(d => ({ id: d.id, ...d.data() }))
        })
    }

    function casesByStatus(status, companyId) {
        return cases.value.filter(c =>
            c.status === status && (companyId == null || c.companyId === companyId)
        )
    }

    function statusCount(status, companyId) {
        return casesByStatus(status, companyId).length
    }

    const activeCases = computed(() =>
        cases.value.filter(c => !['completed', 'lost'].includes(c.status))
    )

    async function addCase(data) {
        return addDoc(collection(db, 'cases'), { ...data, createdAt: serverTimestamp() })
    }

    async function updateCase(id, data) {
        return updateDoc(doc(db, 'cases', id), data)
    }

    async function deleteCase(id) {
        const taskSnap = await getDocs(collection(db, 'cases', id, 'tasks'))
        await Promise.all(taskSnap.docs.map(d => deleteDoc(d.ref)))
        await deleteDoc(doc(db, 'cases', id))
    }

    return { cases, activeCases, subscribe, casesByStatus, statusCount, addCase, updateCase, deleteCase }
})
```

- [ ] **Step 2：確認語法正確（不執行，目視檢查 import 行有無遺漏）**

確認第 4 行 import 包含：`deleteDoc, getDocs`

---

## Task 2：CaseEditModal.vue 刪除按鈕

**Files:**
- Modify: `naiship-system/src/components/cases/CaseEditModal.vue`

- [ ] **Step 1：在 `<script setup>` 區塊新增 confirmDelete、deleting ref 和 deleteThisCase 函式**

在 `const saving = ref(false)` 下方加入：

```js
const confirmDelete = ref(false)
const deleting = ref(false)

async function deleteThisCase() {
    if (deleting.value) return
    deleting.value = true
    try {
        await casesStore.deleteCase(props.caseId)
        emit('close')
    } finally {
        deleting.value = false
    }
}
```

- [ ] **Step 2：在 template 底部 `<div class="flex justify-end gap-2 mt-5">` 前插入刪除區塊**

將原本的：
```html
      <div class="flex justify-end gap-2 mt-5">
        <button @click="$emit('close')" class="text-sm text-gray-400 px-4 py-2">取消</button>
        <button @click="save" :disabled="saving || !caseData" class="text-sm text-white px-5 py-2 rounded-xl disabled:opacity-60" style="background:#1e2533">
          {{ saving ? '儲存中…' : '儲存' }}
        </button>
      </div>
```

換成：
```html
      <!-- 刪除區域 -->
      <div v-if="caseData" class="mt-5 pt-4 border-t border-gray-100">
        <div v-if="!confirmDelete" class="flex justify-between items-center">
          <button @click="confirmDelete = true" class="text-xs text-red-400 hover:text-red-600">刪除此案件</button>
          <div class="flex gap-2">
            <button @click="$emit('close')" class="text-sm text-gray-400 px-4 py-2">取消</button>
            <button @click="save" :disabled="saving" class="text-sm text-white px-5 py-2 rounded-xl disabled:opacity-60" style="background:#1e2533">
              {{ saving ? '儲存中…' : '儲存' }}
            </button>
          </div>
        </div>
        <div v-else class="flex flex-col gap-3">
          <p class="text-sm text-red-600 font-medium">⚠ 確定刪除「{{ caseData.name }}」？此操作無法復原。</p>
          <div class="flex gap-2 justify-end">
            <button @click="confirmDelete = false" class="text-sm text-gray-400 px-4 py-2">取消</button>
            <button @click="deleteThisCase" :disabled="deleting" class="text-sm text-white px-5 py-2 rounded-xl bg-red-500 hover:bg-red-600 disabled:opacity-60">
              {{ deleting ? '刪除中…' : '確認刪除' }}
            </button>
          </div>
        </div>
      </div>
      <div v-else class="flex justify-end gap-2 mt-5">
        <button @click="$emit('close')" class="text-sm text-gray-400 px-4 py-2">取消</button>
      </div>
```

- [ ] **Step 3：重置 confirmDelete 當 caseId 改變時（在 `watch(caseData, ...)` 內）**

在 `watch(caseData, (c) => {` 的函式最開始加一行：

```js
    confirmDelete.value = false
```

- [ ] **Step 4：在瀏覽器測試**

1. 打開任一案件的編輯 Modal
2. 確認底部有「刪除此案件」小按鈕（左下角）
3. 點擊後出現警告文字和「確認刪除」按鈕
4. 點「取消」回到原狀態
5. 點「確認刪除」→ Modal 關閉，側欄該案件消失

- [ ] **Step 5：Commit**

```bash
git add naiship-system/src/stores/cases.js naiship-system/src/components/cases/CaseEditModal.vue
git commit -m "feat(案件): 新增刪除功能，CaseEditModal 二次確認防呆"
```

---

## Task 3：PaymentMilestones.vue 套用模板

**Files:**
- Modify: `naiship-system/src/components/cases/PaymentMilestones.vue`

- [ ] **Step 1：在 `<script setup>` 新增 template 相關 ref 和函式**

在 `const showForm = ref(false)` 下方加入：

```js
const TEMPLATE_LABELS = ['訂金（簽約款）', '第二期款', '第三期款', '第四期款', '第五期款', '尾款']

const showTemplate = ref(false)
const templateRows = ref([])

function openTemplate() {
    templateRows.value = TEMPLATE_LABELS.map(label => ({ label, amount: 0, dueDate: '' }))
    showTemplate.value = true
}

async function submitTemplate() {
    if (saving.value) return
    const rows = templateRows.value.filter(r => (r.amount || 0) > 0)
    if (rows.length === 0) { showTemplate.value = false; return }
    saving.value = true
    try {
        const newEntries = rows.map(r => ({
            id: `pm_${Date.now()}_${Math.random().toString(36).slice(2)}`,
            label: r.label,
            amount: r.amount,
            dueDate: r.dueDate || '',
            paidAmount: 0,
            paidDate: '',
        }))
        const updated = [...milestones.value, ...newEntries].slice(0, 6)
        await casesStore.updateCase(props.caseId, { paymentMilestones: updated })
        showTemplate.value = false
    } finally {
        saving.value = false
    }
}
```

- [ ] **Step 2：在標題列新增「套用模板」按鈕**

找到：
```html
        <button v-if="milestones.length < 6" @click="openAdd"
          class="text-xs px-3 py-1.5 rounded-lg text-white" style="background:#1e2533">+ 新增期款</button>
```

換成：
```html
        <button v-if="milestones.length < 6" @click="openTemplate"
          class="text-xs px-3 py-1.5 rounded-lg border border-gray-200 text-gray-600 hover:bg-gray-50">套用模板</button>
        <button v-if="milestones.length < 6" @click="openAdd"
          class="text-xs px-3 py-1.5 rounded-lg text-white" style="background:#1e2533">+ 新增期款</button>
```

- [ ] **Step 3：在最後一個 Modal（`<!-- 新增/編輯期款 Modal -->`）後方加模板 Modal**

在 `</template>` 前插入：

```html
  <!-- 套用模板 Modal -->
  <div v-if="showTemplate" class="fixed inset-0 z-50 flex items-center justify-center" style="background:rgba(0,0,0,0.4)">
    <div class="bg-white rounded-2xl shadow-xl p-6 w-full max-w-lg mx-4 max-h-[90vh] overflow-y-auto">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-sm font-bold text-gray-800">套用收款模板</h3>
        <button @click="showTemplate = false" class="text-gray-400 hover:text-gray-600 text-lg leading-none">✕</button>
      </div>
      <p class="text-xs text-gray-400 mb-4">金額填 0 的期款將略過不建立</p>
      <div class="flex flex-col gap-2">
        <div v-for="(row, i) in templateRows" :key="i" class="grid grid-cols-3 gap-2 items-center">
          <span class="text-xs text-gray-700 font-medium">{{ row.label }}</span>
          <input v-model.number="row.amount" type="number" min="0" placeholder="金額（0=略過）"
            class="text-sm border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none focus:ring-1">
          <input :value="row.dueDate" type="date"
            @input="row.dueDate = $event.target.value"
            @change="row.dueDate = $event.target.value"
            class="text-sm border border-gray-200 rounded-lg px-3 py-1.5 focus:outline-none focus:ring-1">
        </div>
      </div>
      <div class="flex justify-end gap-2 mt-5">
        <button @click="showTemplate = false" class="text-sm text-gray-400 px-4 py-2">取消</button>
        <button @click="submitTemplate" :disabled="saving" class="text-sm text-white px-5 py-2 rounded-xl disabled:opacity-60" style="background:#1e2533">
          {{ saving ? '建立中…' : '建立期款' }}
        </button>
      </div>
    </div>
  </div>
```

- [ ] **Step 4：在瀏覽器測試**

1. 開一個案件，展開收款期程面板
2. 確認有「套用模板」按鈕
3. 點開 Modal，6 行全顯示
4. 第一行填 300000，第六行填 200000，其餘留 0
5. 點「建立期款」→ 只建立 2 筆，Modal 關閉

- [ ] **Step 5：Commit**

```bash
git add naiship-system/src/components/cases/PaymentMilestones.vue
git commit -m "feat(收款): 新增 6 期批次模板，金額 0 自動略過"
```

---

## Task 4：GanttTab.vue 狀態篩選 chip

**Files:**
- Modify: `naiship-system/src/components/cases/GanttTab.vue`

- [ ] **Step 1：在 `<script setup>` 新增 activeStatuses ref**

在 `const STATUS_BAR_COLORS = {` 前加：

```js
const ALL_STATUSES = ['negotiating', 'drafting', 'construction', 'pending_settlement', 'aftercare', 'completed', 'lost']
const activeStatuses = ref(new Set(ALL_STATUSES))

function toggleStatus(s) {
    const next = new Set(activeStatuses.value)
    if (next.has(s)) { next.delete(s) } else { next.add(s) }
    activeStatuses.value = next
}
```

- [ ] **Step 2：更新 regionCases computed 加入狀態過濾**

找到：
```js
const regionCases = computed(() =>
    casesStore.cases
        .filter(c => c.companyId === props.region)
        .map(c => ({ ...c, ganttBar: getCaseGanttBar(c, displayYear.value, displayMonth.value) }))
)
```

換成：
```js
const regionCases = computed(() =>
    casesStore.cases
        .filter(c => c.companyId === props.region && activeStatuses.value.has(c.status))
        .map(c => ({ ...c, ganttBar: getCaseGanttBar(c, displayYear.value, displayMonth.value) }))
)
```

- [ ] **Step 3：在 template 的甘特表格前插入 chip 列**

找到：
```html
    <div class="bg-white rounded-2xl shadow-sm overflow-hidden">
      <div class="flex" style="min-height:200px">
```

在這兩行之間插入 chip 列：

```html
    <div class="bg-white rounded-2xl shadow-sm overflow-hidden">
      <!-- 狀態篩選 chips -->
      <div class="px-4 py-2 border-b border-gray-100 flex items-center gap-1.5 flex-wrap">
        <span class="text-[11px] text-gray-400 font-semibold mr-1">篩選：</span>
        <button v-for="[status, color] in statusLegend" :key="status"
          @click="toggleStatus(status)"
          class="flex items-center gap-1 px-2 py-1 rounded-full text-[11px] border transition-colors"
          :style="activeStatuses.has(status)
            ? `background:${color};color:#fff;border-color:${color}`
            : 'background:#f9fafb;color:#9ca3af;border-color:#e5e7eb'">
          {{ STATUS_LABELS[status] }}
        </button>
      </div>
      <div class="flex" style="min-height:200px">
```

- [ ] **Step 4：在瀏覽器測試**

1. 切到「案件進度」tab
2. 確認頂部有狀態 chip，全部預設高亮
3. 點「洽談中」chip → 取消該狀態，洽談中案件消失
4. 再點一次 → 恢復

- [ ] **Step 5：Commit**

```bash
git add naiship-system/src/components/cases/GanttTab.vue
git commit -m "feat(甘特圖): 新增狀態篩選 chip，多選切換"
```

---

## Task 5：WorkTypePanel.vue 付款進度顏色

**Files:**
- Modify: `naiship-system/src/components/cases/WorkTypePanel.vue`

- [ ] **Step 1：更新廠商工程款欄位的「已付」顯示，加入顏色邏輯**

找到（WorkTypePanel.vue 約第 59-61 行）：
```html
              <div v-if="totalVendorPaid(wt) > 0" class="text-[10px] text-gray-400 mt-0.5">
                已付 ${{ totalVendorPaid(wt).toLocaleString() }}
              </div>
```

換成：
```html
              <div v-if="wt.vendorCost > 0" class="text-[10px] mt-0.5"
                :class="totalVendorPaid(wt) >= wt.vendorCost ? 'text-green-600'
                      : totalVendorPaid(wt) > 0 ? 'text-orange-500'
                      : 'text-gray-400'">
                已付 ${{ totalVendorPaid(wt).toLocaleString() }}
                <span v-if="totalVendorPaid(wt) >= wt.vendorCost" class="ml-0.5">✓</span>
              </div>
```

- [ ] **Step 2：在瀏覽器測試**

1. 進任一施工中案件，展開工種面板
2. 確認有廠商工程款的工種，「已付」顯示顏色正確：
   - 未付：灰色
   - 部分付：橘色
   - 付清：綠色 + ✓

- [ ] **Step 3：Commit**

```bash
git add naiship-system/src/components/cases/WorkTypePanel.vue
git commit -m "feat(工種): 廠商付款進度顏色標示（灰/橘/綠）"
```

---

## Task 6：客戶跟進加入行事曆

**Files:**
- Modify: `naiship-system/src/components/clients/ClientDetail.vue`
- Modify: `naiship-system/src/components/cases/CalendarTab.vue`

### 6a：ClientDetail.vue 加按鈕

- [ ] **Step 1：新增 import 和 addToCalendar 函式**

找到 `ClientDetail.vue` script setup 區的 import 區塊（包含 `useClientsStore`、`useCasesStore` 等），加入：

```js
import { Timestamp } from 'firebase/firestore'
import { useCalendarEventsStore } from '@/stores/calendarEvents'
import { useAuthStore } from '@/stores/auth'
```

在 `const { toast } = useToast()` 下方加：

```js
const eventsStore = useCalendarEventsStore()
const authStore = useAuthStore()
const addingToCalendar = ref(false)

async function addToCalendar() {
    if (!props.client?.followUpDate || addingToCalendar.value) return
    addingToCalendar.value = true
    try {
        await eventsStore.addEvent({
            companyId: props.client.companyId,
            type: 'followup',
            date: Timestamp.fromDate(new Date(props.client.followUpDate)),
            label: `跟進：${props.client.name}`,
            createdBy: authStore.user?.uid ?? '',
        })
        toast('已加入行事曆')
    } finally {
        addingToCalendar.value = false
    }
}
```

- [ ] **Step 2：在「下次跟進」欄旁加按鈕**

找到（ClientDetail.vue template 約第 24 行）：
```html
        <div>
          <span class="text-gray-400 text-xs block mb-0.5">下次跟進</span>
          <p class="text-gray-700">{{ client.followUpDate || '—' }}</p>
        </div>
```

換成：
```html
        <div>
          <span class="text-gray-400 text-xs block mb-0.5">下次跟進</span>
          <div class="flex items-center gap-2">
            <p class="text-gray-700">{{ client.followUpDate || '—' }}</p>
            <button v-if="client.followUpDate" @click="addToCalendar" :disabled="addingToCalendar"
              class="text-[10px] border border-purple-200 rounded px-1.5 py-0.5 text-purple-600 hover:bg-purple-50 disabled:opacity-50">
              {{ addingToCalendar ? '…' : '＋行事曆' }}
            </button>
          </div>
        </div>
```

### 6b：CalendarTab.vue 加 followup 類型

- [ ] **Step 3：在 eventTypes 陣列加 followup**

找到：
```js
const eventTypes = [
  { key: 'milestone', label: '案件里程碑', color: '#c9a96e' },
  { key: 'leave',     label: '員工請假',   color: '#60a5fa' },
  { key: 'note',      label: '重要記事',   color: '#f87171' },
]
```

換成：
```js
const eventTypes = [
  { key: 'milestone', label: '案件里程碑', color: '#c9a96e' },
  { key: 'leave',     label: '員工請假',   color: '#60a5fa' },
  { key: 'note',      label: '重要記事',   color: '#f87171' },
  { key: 'followup',  label: '客戶跟進',   color: '#a855f7' },
]
```

- [ ] **Step 4：在行事曆格事件渲染加 followup 顏色**

找到：
```html
          :class="event.type === 'leave' ? 'bg-blue-400' : event.type === 'note' ? 'bg-red-400' : ''"
          :style="event.type === 'milestone' ? 'background:#c9a96e' : ''">
```

換成：
```html
          :class="event.type === 'leave' ? 'bg-blue-400' : event.type === 'note' ? 'bg-red-400' : ''"
          :style="event.type === 'milestone' ? 'background:#c9a96e' : event.type === 'followup' ? 'background:#a855f7' : ''">
```

- [ ] **Step 5：在圖例列加客戶跟進**

找到：
```html
        <div class="flex items-center gap-1.5"><span class="w-3 h-3 rounded bg-red-400"></span>重要記事</div>
```

後面加：
```html
        <div class="flex items-center gap-1.5"><span class="w-3 h-3 rounded" style="background:#a855f7"></span>客戶跟進</div>
```

- [ ] **Step 6：在瀏覽器測試**

1. 進客戶管理，選一個有「下次跟進」日期的客戶
2. 確認「下次跟進」欄旁有「＋行事曆」按鈕
3. 點按鈕 → toast「已加入行事曆」
4. 進案件管理 → 行事曆，確認對應日期出現紫色「跟進：{客戶名}」事件

- [ ] **Step 7：Commit**

```bash
git add naiship-system/src/components/clients/ClientDetail.vue naiship-system/src/components/cases/CalendarTab.vue
git commit -m "feat(客戶): 下次跟進一鍵加入行事曆，顯示紫色事件"
```

---

## Task 7：VendorManager.vue 廠商評分與備注

**Files:**
- Modify: `naiship-system/src/components/settings/VendorManager.vue`

- [ ] **Step 1：更新 blankForm 和 openEdit**

找到：
```js
const blankForm = () => ({ name: '', specialty: '', contact: '', phone: '', taxId: '', lineId: '', formSubmitted: false, formDate: '', companyId: '' })
```

換成：
```js
const blankForm = () => ({ name: '', specialty: '', contact: '', phone: '', taxId: '', lineId: '', formSubmitted: false, formDate: '', companyId: '', rating: 0, notes: '' })
```

找到 `openEdit` 函式中的 `form.value = {` 區塊，新增最後兩行：

```js
function openEdit(v) {
    editingId.value = v.id
    form.value = {
        name: v.name, specialty: v.specialty,
        contact: v.contact || '', phone: v.phone || '',
        taxId: v.taxId || '', lineId: v.lineId || '',
        formSubmitted: v.formSubmitted || false, formDate: v.formDate || '',
        companyId: v.companyId || '',
        rating: v.rating || 0,
        notes: v.notes || '',
    }
    showForm.value = true
}
```

- [ ] **Step 2：在廠商表格新增「評分」欄**

找到表格 `<thead>` 的最後一個 `<th>`（操作欄）前插入：

找到：
```html
            <th class="text-center px-3 py-2 text-gray-500 font-semibold w-16">操作</th>
```

前面加：
```html
            <th class="text-center px-3 py-2 text-gray-500 font-semibold">評分</th>
```

在表格 `<tbody>` 中，找到操作欄 `<td class="px-3 py-2.5 text-center">` 前插入評分欄：

找到（廠商資料表 td 後的操作 td）：
```html
            <td class="px-3 py-2.5 text-center">
              <div class="flex items-center justify-center gap-2">
                <button @click="openEdit(v)" class="text-gray-400 hover:text-gray-700">編輯</button>
```

前面插入：
```html
            <td class="px-3 py-2.5 text-center text-xs text-gray-600">
              {{ v.rating ? '★'.repeat(v.rating) + '☆'.repeat(5 - v.rating) : '—' }}
            </td>
```

- [ ] **Step 3：在 Modal 表單加評分和備注欄位**

找到 `<div class="flex flex-col gap-3">` 這個 Modal 表單容器，其內最後一個子元素是已繳交廠商資料表的 `<div class="border border-gray-100 rounded-xl p-3 bg-gray-50/50">...</div>`。在這個子元素的結束 `</div>` 後、flex-col 容器的結束 `</div>` 前插入：

```html
        <div>
          <label class="text-xs text-gray-500 mb-2 block">廠商評分</label>
          <div class="flex gap-1">
            <button v-for="n in 5" :key="n" @click="form.rating = form.rating === n ? 0 : n"
              class="text-xl transition-colors"
              :class="n <= form.rating ? 'text-amber-400' : 'text-gray-200 hover:text-amber-300'">★</button>
          </div>
        </div>
        <div>
          <label class="text-xs text-gray-500 mb-1 block">備注</label>
          <textarea v-model="form.notes" rows="3"
            class="w-full text-sm border border-gray-200 rounded-lg px-3 py-2 focus:outline-none focus:ring-1 resize-none"
            placeholder="廠商合作評價、注意事項…"></textarea>
        </div>
```

- [ ] **Step 4：在瀏覽器測試**

1. 進系統設定 → 廠商管理
2. 確認表格有「評分」欄
3. 點「編輯」現有廠商，Modal 有星星評分和備注
4. 點 3 顆星 → 儲存 → 表格顯示「★★★☆☆」

- [ ] **Step 5：Commit**

```bash
git add naiship-system/src/components/settings/VendorManager.vue
git commit -m "feat(廠商): 新增評分（1-5星）與備注欄位"
```

---

## Task 8：WorkJournalTab.vue 週報匯出 Excel

**Files:**
- Modify: `naiship-system/src/components/cases/WorkJournalTab.vue`

- [ ] **Step 1：新增 xlsx import 和 exportWeekLogs 函式**

在 `<script setup>` 的 import 區塊加：

```js
import * as XLSX from 'xlsx'
```

在 `async function submitReply(logId)` 前加：

```js
function exportWeekLogs() {
    if (displayedLogs.value.length === 0) {
        alert('本週無工作日誌記錄')
        return
    }
    const rows = []
    displayedLogs.value.forEach(log => {
        const dateStr = log.date
            ? (() => { const d = log.date.toDate?.() ?? new Date(log.date); return `${d.getFullYear()}/${String(d.getMonth()+1).padStart(2,'0')}/${String(d.getDate()).padStart(2,'0')}` })()
            : ''
        const caseReport = log.caseEntries?.length
            ? log.caseEntries.map(e => `${e.caseName}：${e.content}`).join('\n')
            : (log.content || '')
        const otherWork = log.otherItems?.map(i => i.content).join('\n') || ''
        const fuelKm = log.fuelExpenses?.reduce((s, f) => s + (f.distance || 0), 0)
            ?? (log.fuelExpense?.distance || 0)
        rows.push({
            '員工姓名': log.userName || '',
            '日期': dateStr,
            '案件回報': caseReport,
            '其他工作': otherWork,
            '油資(km)': fuelKm || '',
            '油資($)': fuelKm ? fuelKm * 6 : '',
        })
    })
    const ws = XLSX.utils.json_to_sheet(rows)
    const wb = XLSX.utils.book_new()
    XLSX.utils.book_append_sheet(wb, ws, '工作日誌')
    const ws = getWeekStart(selectedDate.value)
    const we = getWeekEnd(selectedDate.value)
    const fmt = d => `${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}`
    XLSX.writeFile(wb, `工作日誌_${fmt(ws)}_${fmt(we)}.xlsx`)
}
```

> ⚠ 注意：上面 `const ws` 宣告了兩次（xlsx worksheet 和 weekStart），需重命名其中一個。

修正版（避免變數衝突）：

```js
function exportWeekLogs() {
    if (displayedLogs.value.length === 0) {
        alert('本週無工作日誌記錄')
        return
    }
    const rows = []
    displayedLogs.value.forEach(log => {
        const dateStr = log.date
            ? (() => { const d = log.date.toDate?.() ?? new Date(log.date); return `${d.getFullYear()}/${String(d.getMonth()+1).padStart(2,'0')}/${String(d.getDate()).padStart(2,'0')}` })()
            : ''
        const caseReport = log.caseEntries?.length
            ? log.caseEntries.map(e => `${e.caseName}：${e.content}`).join('\n')
            : (log.content || '')
        const otherWork = log.otherItems?.map(i => i.content).join('\n') || ''
        const fuelKm = log.fuelExpenses?.reduce((s, f) => s + (f.distance || 0), 0)
            ?? (log.fuelExpense?.distance || 0)
        rows.push({
            '員工姓名': log.userName || '',
            '日期': dateStr,
            '案件回報': caseReport,
            '其他工作': otherWork,
            '油資(km)': fuelKm || '',
            '油資($)': fuelKm ? fuelKm * 6 : '',
        })
    })
    const sheet = XLSX.utils.json_to_sheet(rows)
    const wb = XLSX.utils.book_new()
    XLSX.utils.book_append_sheet(wb, sheet, '工作日誌')
    const weekStart = getWeekStart(selectedDate.value)
    const weekEnd = getWeekEnd(selectedDate.value)
    const fmt = d => `${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}`
    XLSX.writeFile(wb, `工作日誌_${fmt(weekStart)}_${fmt(weekEnd)}.xlsx`)
}
```

- [ ] **Step 2：在週檢視模式加「匯出本週」按鈕**

找到（WorkJournalTab.vue template，工具列區塊）：

```html
          <button v-if="isToday" @click="openLogForm" class="text-xs text-white px-3 py-1.5 rounded-lg" style="background:#1e2533">+ 填寫今日日誌</button>
```

前面加：

```html
          <button v-if="viewMode === 'week'" @click="exportWeekLogs"
            class="text-xs border border-gray-200 rounded-lg px-3 py-1.5 text-gray-500 hover:border-gray-400">匯出本週</button>
```

- [ ] **Step 3：在瀏覽器測試**

1. 進工作日誌，切到「週檢視」
2. 確認右上有「匯出本週」按鈕
3. 點擊 → 下載 xlsx 檔案
4. 打開 Excel 確認欄位正確

- [ ] **Step 4：Commit**

```bash
git add naiship-system/src/components/cases/WorkJournalTab.vue
git commit -m "feat(工作日誌): 週檢視新增匯出本週 Excel 功能"
```

---

## 最終驗收

- [ ] 開啟 dev server（`npm run dev`）
- [ ] 逐一測試 7 個功能
- [ ] 確認沒有 console error
- [ ] 整體 commit 確認

```bash
git log --oneline -8
```

預期看到 7 個新 commit（Tasks 2~8 各一個）加上 Task 1 與 Task 2 合併的 commit。
